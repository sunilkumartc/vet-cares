import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function MissedSalesManager() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Missed Sales</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Functionality to manage missed sales will be implemented here.</p>
      </CardContent>
    </Card>
  );
}